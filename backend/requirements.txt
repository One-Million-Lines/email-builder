# AI service for the OpenPostcards email builder.
# Only Flask + flask-cors are required; the model client uses the Python stdlib
# (urllib) so there is no hard dependency on any AI SDK.
flask>=3.0
flask-cors>=4.0
